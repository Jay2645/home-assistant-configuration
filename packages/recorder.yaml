recorder:
  db_url: !secret mysql_database_info
  purge_keep_days: 4
  exclude:
    domains:
      - automation
      - calendar
      - device_tracker
      - group
      - script
      - input_boolean
      - input_number
      - input_select
      - zone
    entities:
      - sun.sun
      - sensor.date_time
